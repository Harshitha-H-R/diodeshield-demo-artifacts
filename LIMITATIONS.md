# Limitations

Status labels: **implemented** means runnable in this repository; **prototype** means
functional but not validated for production; **experimental** means optional research
interfaces; **planned** means not included. No performance, detection, calibration,
or CVE claims are made without measured labelled data. Absence of traffic is reported
as visibility loss, never as proof of a failed physical diode.
